[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

# WiFi.h and HTTPClient.h ship with the espressif32 Arduino framework,
# so no lib_deps are required. The firmware includes secrets.h for WiFi
# credentials; create it from secrets.example.h before building.
