[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

lib_deps =
    # WiFi and HTTPClient are built-in, but listing them explicitly is good practice
    WiFi
    HTTPClient
